import React from 'react';

const ParaGraphtext = ({text}) => (
    <div className="lead text-white mb-4 p-1 a-ParaGraph_background-transparent">
        {text}
    </div>
)

export default ParaGraphtext;